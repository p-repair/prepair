<!DOCTYPE html>
<html lang="en" style="scrollbar-gutter: stable;">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <!-- Include the Tailwind CSS stylesheet -->
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <title>(p)repair</title>
  </head>
  <body class="bg-white">
     <div class="container mx-auto p-4">
      <h1 class="text-3xl font-bold mb-6">Newsletter de (p)repair</h1>
      <p class="mb-8">Ensemble, (p)réparons notre avenir.</p>
      <.flash_group flash={@flash} />
      <.form let={f} for={@changeset} action="/subscribe" class="max-w-[50%] mx-auto">
        <div class="flex items-end border-b-2 border-gray-300 py-4">
          <.input field={f[:email]} type="email" placeholder="Entrez votre adresse email"/>
          <div class="ml-2"><.button>
            S’inscrire
          </.button>
          </div>
        </div>
      </.form>
    </div>
  </body>
</html>
