<!DOCTYPE html>
<html lang="en" style="scrollbar-gutter: stable;">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <!-- Include the Tailwind CSS stylesheet -->
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <title>(p)repair</title>
  </head>
  <body class="bg-white">
     <div class="container mx-auto p-4 ">
      <h1 class="text-3xl font-bold mb-4">Newsletter de (p)repair</h1>
      <p class="mb-8">Ensemble, (p)réparons notre avenir.</p>
      <.flash_group flash={@flash} />
      <.form let={f} for={@changeset} action="/subscribe" class="max-w-sm mx-auto">
        <div class="flex items-center border-b border-b-2 border-gray-500 py-2">
          <.input field={f[:email]} type="text" placeholder="Entrez votre adresse électronique" class="appearance-none bg-transparent border-none w-full text-gray-700 mr-3 py-1 px-2 leading-tight focus:outline-none"/>
          <button class="flex-shrink-0 bg-blue-500 hover:bg-blue-700 border-blue-500 hover:border-blue-700 text-sm border-4 text-white py-1 px-2 rounded" type="submit">
            S’inscrire
          </button>
        </div>
      </.form>
    </div>
  </body>
</html>
